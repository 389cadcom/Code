{
  "name": "ts",
  "version": "1.0.0",
  "description": "nodemon参数  --ext  exec",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --ext ts --watch server --exec \"npm run clean && npm run tsc && npm run server\"",
    "server": "cross-env NODE_ENV=development node ./dist/app.js",
    "clean": "rm -rf ./dist",
    "tsc": "tsc",
    "build": "npm run clean && npm run tsc"
  },
  "author": "",
  "license": "ISC"
}
